package garr9903;

import java.util.Set;
import java.util.UUID;


public class GlobalShipState {
	/*void setTarget(Toroidal2DPhysics space){
		Ship self = (Ship) space.getObjectById(ship);
		Set<Asteroid> asteroids = space.getAsteroids();
		double best = Double.MIN_VALUE;
		for(Asteroid ast : asteroids){
			if(ast.getResources().getTotal() / space.findShortestDistance(self.getPosition(), ast.getPosition()) > best){
				target = ast;
			}
		}
	}*/
}
